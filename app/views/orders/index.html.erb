<p id="notice"><%= notice %></p>

<h1>Commandes</h1>

<table>
  <thead>
    <tr>
      <th>Nom du client</th>
      <th>Adresse</th>
      <th>Pizzas commandées</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        	<td> <%= order[:order][:client_name]  %>
	        <td> <%= order[:order][:adress] %>
	        <td> <table border=1 >
		        <% order[:pizzas].each do |pizza| %>
                     <tr><td> <%= pizza[:name] %> <td> <img src="<%= pizza[:url] %>" width="60px" height="60px" />
                <% end %>
	             </table>
      </tr>
    <% end %>
  </tbody>
</table>
